#!/usr/bin/env python3
"""Run the current complete Graphium non-desktop verification."""
from __future__ import annotations

from pathlib import Path
import runpy

ROOT = Path(__file__).resolve().parents[1]
runpy.run_path(str(ROOT / "VERIFY_GRAPHIUM.py"), run_name="__main__")
